# Nhost Configuration
# Copy this file to .env and replace the placeholder values with your actual Nhost credentials

# Option 1: Use subdomain and region
VITE_NHOST_SUBDOMAIN=your-subdomain
VITE_NHOST_REGION=eu-central-1
# Option 2: Use backend URL instead of subdomain+region
# VITE_NHOST_BACKEND_URL=https://your-subdomain.nhost.run

# Example:
# VITE_NHOST_SUBDOMAIN=myapp
# VITE_NHOST_REGION=eu-central-1
# VITE_NHOST_BACKEND_URL=https://myapp.nhost.run
